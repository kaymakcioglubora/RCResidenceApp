import { React } from "react";
import { 
    View, 
    Text,
    StyleSheet,
    SafeAreaView,
    Image,
    TouchableOpacity,
    TextInput,
    Pressable,
} from "react-native";

import { COLORS, FONTS, SIZES, icons, images } from "../../constants/";

import CalendarPicker from "react-native-calendar-picker";

const InResidence = () => {
    return (
        <View>
            <Text>InResidence</Text>
        </View>
    )
}

export default InResidence;